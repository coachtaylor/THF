/**
 * TransFitness Knowledge Engine
 *
 * A licensable, separable module containing:
 * - Safety rules (TypeScript logic, version-controlled)
 * - Knowledge content (Supabase, exportable)
 * - Safety guides (Supabase, exportable)
 *
 * Part of the Platform Evolution Strategy - Phase 2
 */

// Types
export * from './types';

// Content (Database-backed)
export * from './content';

// Rules (TypeScript logic)
export * from './rules';
